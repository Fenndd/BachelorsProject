# Global default settings placeholder.
# This is not a full experiment configuration system yet.

project:
  name: p3p-llm-optimization
  stage: minimal-baseline-automation

paths:
  workspace_dir: workspace
  results_dir: results
