/* ===================================================================
   views.tcss -- content area and view-specific styling
   =================================================================== */

#content {
    background: #ffffff;
    padding: 1 2;
    height: 1fr;
}

#content > * {
    height: 1fr;
}

/* ---- Actions button row ---- */

.actions {
    layout: grid;
    grid-size: 2;
    grid-gutter: 1;
    margin-top: 1;
}

.field-label {
    margin-top: 1;
    color: #1f2937;
    text-style: bold;
}

#content Button {
    background: #e5e7eb;
    color: #111827;
    border: tall #9ca3af;
    text-style: bold;
}

#content Button:hover {
    background: #d1d5db;
    color: #111827;
}

#content Button:disabled {
    background: #f3f4f6;
    color: #6b7280;
}

/* ---- Results view: filter row ---- */

#results-view {
    height: 1fr;
}

#filter-row {
    height: auto;
    margin-bottom: 1;
}

#filter-row Input {
    width: 1fr;
    margin-right: 1;
}

#filter-row Select {
    width: 20;
}

/* ---- Results view: DataTable ---- */

DataTable {
    background: #ffffff;
    color: #1f2937;
}

#results-table {
    height: 1fr;
    min-height: 6;
}

DataTable > .datatable--header {
    background: #eef0f3;
    color: #1f2937;
}

DataTable > .datatable--cursor {
    background: #dbeafe;
    color: #1f2937;
}

/* ---- Results view: summary and status ---- */

#result-summary-text {
    min-height: 10;
    height: auto;
}

#results-status {
    min-height: 1;
    height: auto;
}

/* ---- Experiment view ---- */

#experiment-list {
    min-height: 5;
}

#experiment-summary {
    height: auto;
}

/* ---- Active run live log screen ---- */

#run-status {
    height: auto;
    min-height: 6;
    margin-bottom: 1;
}

#run-log {
    height: 1fr;
    min-height: 8;
    margin-bottom: 1;
}

.run-action-row {
    layout: grid;
    grid-size: 2;
    grid-gutter: 1;
    margin-top: 1;
}

.run-action-button {
    width: 100%;
}
